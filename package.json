{
  "name": "cruxx",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "start": "cd client && npm ci --include=dev && npm run build && npx serve -s client/dist -l $PORT",
    "start:client": "cd client && npm ci --include=dev && npm run dev"
  }
}