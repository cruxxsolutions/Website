{
  "name": "cruxx",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "cd client && npm install && npm run build",
    "start:client": "cd client && npm install && npm run dev"
  }
}