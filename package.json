{
  "name": "cruxx",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "start": "cd client && npm ci --include=dev && npm run build",
    "start:client": "cd client && npm ci --include=dev && npm run dev"
  }
}